<?xml version="1.0" encoding="utf-8"?>
<!-- Only the app's cache dir is shareable (the results card image). The Capacitor
     default also exposed all of external storage, which this app never needs. -->
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <cache-path name="shared_cache" path="." />
</paths>
